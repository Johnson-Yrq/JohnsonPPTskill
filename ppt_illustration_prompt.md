你是一名企业级 PPT 信息可视化设计师，同时擅长三维场景设计。请阅读末尾的「配图信息」与「正文」，先在内部完成内容理解、要点提炼与场景构思，再直接生成一张用于现有 PPT 页面的三维场景化配图。

本次仅生成配图，不包含任何文字、标题、说明、标签或文字框。配图将放入现有 PPT 页面的指定区域，与页面上已有的标题、正文和说明并列出现，因此配图只负责把正文中最核心的一个要点视觉化，不需要独立解释全部内容，也不承担整页底图的作用。

页面的标题、正文、页尾均已存在于现有 PPT 中，配图不为它们预留任何位置，也不重新生成任何内容。


【生成设置】

画幅：默认 4:3，1600×1200；可按配图信息指定为 16:9、3:2、1:1 或竖版 3:4。
输出模式：纯配图，完全无文字。
视觉风格：三维等距视角、哑光白色建筑黏土模型、暖白纯色背景。
背景模式：默认暖白纯色背景（#F7F6F2）；如配图信息要求透明背景，则输出带透明通道的 PNG，仅保留场景本体、平台与投影。
默认占比：场景本体（含平台与投影）占画布约 60%～75%，四周保留约 6%～8% 的安全边距。

如提供原有 PPT 页面参考、配图位置示意，或指定尺寸与背景，以参考页面或指定要求为准。


一、按配图位置确定画布与留白

配图是页面的一部分，不是整页底图。不设置页头、页尾空白区，不预留标题、副标题、分区标题的位置。

画布四周保留约 6%～8% 的安全边距，所有模型、平台、人物、设施、连接结构及投影完整落在边距之内，不贴边、不出血、不被裁切。

场景外轮廓保持紧凑，重心居中。如配图信息说明了配图相对文字的位置，场景的开放面、操作面或流程走向宜朝向文字一侧，使观众视线在文字与配图之间自然衔接。

同一页面有多张配图时，各图保持相同画幅、相同视角、相同平台尺度和相同的场景占比，方便并排放置。

最终图片保留完整画布，不裁切成场景本体的尺寸；透明背景模式下则只保留场景本体与投影，不带任何背景色块。


二、理解正文，只提炼一个核心要点

配图尺寸有限，且旁边已有文字说明，因此只提炼正文中最核心的一个对象、一个动作或一种关系，不试图把正文的全部内容都画进去。

识别正文的关键对象、主要动作、状态变化和核心关系后，从以下结构中选一种，并用一个场景（确有必要时最多两个紧密关联的节点）表达：

流程：一种对象经过一次处理，处理前后状态可辨。
对比：视角、比例一致的两个并置小场景，通过工作状态或连接方式表现差异。
架构：一个分层、支撑、包含或中心与周边的紧凑结构。
闭环：一条真实首尾相接的短路径。
递进：同一场景由简到全的两个阶段。
并列：权重相当的两个子场景，不强加因果连接。

一张配图只保留一种结构，不叠加辅助关系，不为凑数拆分场景，不堆叠多个小岛。

正文信息较多时，只保留最关键的一处业务对象或一处状态变化；其余内容由页面文字承担。

正文中的标题、术语、数字和总结只用于理解内容，不以文字形式出现在图片中。

不得虚构正文没有的因果、流程、层级、闭环或能力。


三、用场景动作代替文字说明

场景应表现为"正在发生的事情"，而不是一个孤立图标、一栋无关建筑或一个抽象立方体。

优先使用"人物＋业务对象＋操作动作＋状态变化"的组合，让观众一眼看出谁在处理什么、处理前后有什么不同。

根据正文所属行业，选择贴切的工作空间、设备、工具与道具，不机械套用医院、工厂或服务器机房。

可以采用开放式工作台、建筑局部剖切、单段处理设施或小型分层平台，不拘泥于方块房间。

抽象概念尽量转化为可观察的变化，例如：

采集：多个来源的对象进入同一设备或处理区域。
整理：混杂的对象经过处理后变得有序、分组清楚。
审核：人物在工作台核对对象，通过的对象进入下一环节。
协同：不同工作空间共同使用同一设施，或传递同一业务对象。
积累：资料、工具或组件逐渐沉淀，被重复调用。
推广：一个已完成的配置被复用到另一个同类场景。

以上仅为视觉转译方法，不机械加入正文无关的元素。

同一种业务对象保持一致外观，通过位置、组合、完整度或使用状态变化体现流转。

人物应参与操作、交流、核对、搬运或协作，不只是站在旁边充当装饰。

不要把所有能力都画成机器人、齿轮、服务器或球形网络。删除没有解释作用的道具。


四、紧凑构图，服务于页面文字

配图有且只有一个明确的视觉中心，场景占画布约 60%～75%，四周留白干净，不塞满画布。

优先放大主要对象和关键动作，减少次要设施；在 PPT 中缩放到配图尺寸后，主要对象与动作仍应清楚可辨。

场景坐落于一个边缘柔和的悬浮圆角平台上。平台形状简洁，取圆形、圆角矩形或短条形，不做大面积延展、多层分区或复杂拼接。

只有正文确实表达隔离、断裂或孤岛时，才采用互不连接的平台。

关系用位置、距离、朝向、层级、共同底座及业务对象传递来表达。需要连接时，采用与场景协调的实体路径、连廊、轨道或管线，且连接必须对应正文中的真实关系。

不绘制箭头、标注引导线或装饰性连线。没有因果关系的并列对象，不强行连接成流程。

不保留任何原本用于标题、说明、标签或说明卡片的位置，也不留下大片空洞。


五、统一三维建筑黏土模型风格

采用统一的三维等距视角，呈现真实实体比例模型的摄影效果，非卡通，非彩色玩具风格。

所有建筑、设备、机械、家具、道具与人物均为素净的哑光白色或极浅灰色，如同真实白色建筑比例模型。

材质细腻，无明显纹理，不使用金属、玻璃镜面或塑料般的强烈高光。

人物为微小、无五官的白色比例人偶，以自然姿态和动作表达操作、交流、核对与协作。

单色场景只允许两类低饱和度色彩例外：

水面或必要的透明介质使用极浅、低饱和度的灰蓝色。正文不需要水面时，不为装饰加入水域。

少量关键部件使用柔和天蓝色，作为唯一的重点强调色，点缀面积不超过场景的约 10%。

同一份 PPT 的多张配图中，强调色保持相同含义；同一张图内不随意给不同物体染色。

不使用大面积深蓝、红色或其他高饱和度颜色，不将整个平台染成彩色。

采用来自上方的柔和均匀影棚光，细微环境光遮蔽、接触阴影与极淡投影。同一套配图使用同一光照方向。

没有强烈高光、霓虹、发光特效或戏剧化光照。

整体高细节、清晰对焦，具有真实模型的体积、厚度与空间关系，不使用明显景深虚化。

背景为暖白色纸张，参考色 #F7F6F2，带轻微纸张颗粒感，不绘制蓝图网格，以便与页面其他区域融合。仅当配图信息说明页面本身带有蓝图网格背景时，可在平台正下方带极淡的浅灰蓝网格（参考色 #E9EDF8），并向画布边缘自然淡出。

透明背景模式下不绘制任何背景、颗粒或网格，只保留平台下方的柔和投影。

不绘制工程标注、尺寸线、坐标、刻度或复杂蓝图符号。


六、严格禁止任何文字及文字附属元素

整张图片不得出现：

中文、英文、字母、数字、步骤编号；
主标题、副标题、分区标题、说明句、总结句；
对象标签、图例、注释、数据标签；
文字框、说明卡片、空白标题牌、标签底板；
文字引导线、标注锚点、占位符；
品牌标志、页码、水印、签名；
乱码、伪文字、仿文字笔画或装饰性字符。

设备屏幕、文件、档案、标牌和建筑表面也不得出现文字。

确需表现屏幕或文件时，可使用干净表面、简单几何分区或少量非文字图形体现状态，不使用模拟文字的密集横线，不添加漂浮的说明面板。

业务中真实需要的屏幕、纸张和档案可以保留；仅为承载文字而存在的空白框、卡片和牌子必须删除。


七、生成前检查与最终输出

检查画布尺寸与比例是否符合配图信息，背景模式是否正确。

检查所有模型、平台、连接结构及投影是否完整位于安全边距之内，无贴边、无裁切。

检查是否只有一个核心要点、一个视觉中心，缩小后是否仍能一眼看出主要对象与动作。

检查是否残留文字占位、空白说明框、标签底板、标注线、数字或伪文字。

检查场景是否对应正文主线，而不是无关建筑和道具的组合。

检查是否存在过多微小设施或装饰；存在则删减，并适当放大关键对象。

检查与同一套 PPT 其他配图的视角、光照、比例与强调色是否一致。

最终应呈现为：
指定画幅的完整画布；
暖白纯色背景或透明背景；
中央一个紧凑、清楚、无文字的三维场景配图。

直接生成最终图片，不输出分析过程、大纲、设计说明或提示词解释。

不要生成电脑屏幕外框、投影幕布、演示现场或 PPT 软件界面，也不要把配图所在的整页版面或旁边的文字画进图中。最终图片本身就是可直接插入现有 PPT 页面的配图。


【配图信息】（可选，未填写时按默认设置）

所在页面版式：（例如：左文右图、上文下图、三栏并列）
配图位置：（例如：页面右侧约 45% 宽度区域）
画布尺寸或比例：（例如：4:3，1600×1200）
背景模式：（暖白 / 透明）
本页或本套 PPT 的配图数量与编号：（用于保持系列一致）


【正文】

在这里粘贴需要转化为配图的文字。
